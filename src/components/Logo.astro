---
interface Props {
  class?: string;
  style?: string;
}
const { class: className = '', style = '' } = Astro.props;
const base = import.meta.env.BASE_URL;
---
<div class:list={['js-logo', className]} style={style} data-base={base} role="button" tabindex="0" aria-label="Change logo">
</div>
